name: Windows

on:
  push:
    branches: [ "main", "release-**" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build jar
        run: mvn -B package

      - name: Package into exe (app-image)
        shell: pwsh
        run: |
          if (Test-Path build\launcher) { Remove-Item -Recurse -Force build\launcher }
          $mainJar = (Get-ChildItem target -Filter *.jar | Select-Object -First 1).Name
          jpackage --input target/ --name launcher --main-jar $mainJar --main-class chromaforge.launcher.Launcher --type app-image --dest build/ --java-options '-Dfile.encoding=UTF-8' --win-console

      - name: Upload app-image
        uses: actions/upload-artifact@v4
        with:
          name: launcher-windows
          path: build/launcher
          if-no-files-found: error

  test:
    needs: build
    runs-on: windows-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: launcher-windows
          path: launcher

      - run: .\launcher\launcher.exe version
      - run: .\launcher\launcher.exe fetch
      - run: .\launcher\launcher.exe install 0.4.1
      - run: .\launcher\launcher.exe new test 0.4.1
      - run: .\launcher\launcher.exe check 0.4.1
      - run: .\launcher\launcher.exe launch test -- --headless